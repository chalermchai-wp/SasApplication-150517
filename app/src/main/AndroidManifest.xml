<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.realz.sasapplication">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UserActivity"
            android:icon="@drawable/tasks"
            android:label="  เมนู"
            android:theme="@style/AppTheme2" />
        <activity android:name=".TestJSON" />
        <activity
            android:name=".ListAssess"
            android:icon="@drawable/tasks"
            android:label="  รายการแบบประเมิน"
            android:theme="@style/AppTheme2" />
        <activity android:name=".DoneAssess" />
        <activity
            android:name=".ProfileActivity"
            android:label="  ข้อมูลส่วนตัว"
            android:theme="@style/AppTheme2" />
        <activity android:name=".DoAssess"></activity>
    </application>

</manifest>